# SLEO - Структура проекта (JavaScript версия)

## Полная структура

\`\`\`
sleo/
├── frontend/                           # Next.js приложение
│   ├── app/
│   │   ├── page.jsx                   # Страница логина
│   │   ├── layout.jsx                 # Root layout
│   │   ├── globals.css                # Глобальные стили
│   │   └── dashboard/
│   │       ├── page.jsx               # Список волонтеров
│   │       ├── volunteers/
│   │       │   └── [id]/
│   │       │       └── page.jsx       # Профиль волонтера
│   │       └── events/
│   │           ├── page.jsx           # Список событий
│   │           └── [id]/
│   │               └── page.jsx       # Детали события
│   │
│   ├── components/
│   │   ├── navigation.jsx             # Навбар с бургер-меню
│   │   ├── volunteer-form.jsx         # Форма добавления волонтера
│   │   ├── volunteer-list.jsx         # Карточки волонтеров
│   │   ├── event-form.jsx             # Форма добавления события
│   │   ├── event-list.jsx             # Карточки событий
│   │   └── ui/                        # shadcn UI компоненты
│   │       ├── button.jsx
│   │       ├── card.jsx
│   │       ├── input.jsx
│   │       └── ...
│   │
│   ├── package.json                   # Frontend зависимости
│   ├── .env.local                     # Frontend переменные
│   ├── tsconfig.json                  # Next.js конфиг
│   └── next.config.mjs                # Next.js конфиг
│
├── backend/                           # Express.js сервер
│   ├── server.js                      # Основной сервер
│   ├── package.json                   # Backend зависимости
│   └── .env                           # Backend переменные
│
├── SETUP.md                           # Полная инструкция
├── QUICK_START.md                     # Быстрый старт
└── PROJECT_STRUCTURE.md              # Этот файл
\`\`\`

---

## Что где находится?

### Frontend (.jsx файлы)

| Файл | Назначение |
|------|-----------|
| `app/page.jsx` | Экран логина/регистрации |
| `app/dashboard/page.jsx` | Главная страница - список волонтеров |
| `app/dashboard/events/page.jsx` | Страница событий |
| `app/dashboard/volunteers/[id]/page.jsx` | Профиль волонтера |
| `app/dashboard/events/[id]/page.jsx` | Детали события |
| `components/navigation.jsx` | Навигационное меню (адаптивное, бургер-меню на мобилке) |
| `components/volunteer-form.jsx` | Форма для добавления/редактирования волонтера |
| `components/volunteer-list.jsx` | Список волонтеров в виде карточек |
| `components/event-form.jsx` | Форма для добавления/редактирования события |
| `components/event-list.jsx` | Список событий в виде карточек |

### Backend (Node.js)

| Файл | Назначение |
|------|-----------|
| `backend/server.js` | Express сервер с MongoDB, JWT, API endpoints |
| `backend/.env` | Переменные окружения (MongoDB URI, JWT Secret, Port) |
| `backend/package.json` | Зависимости (express, mongoose, bcryptjs, jsonwebtoken, cors) |

---

## Технологический стек

### Frontend
- **Framework:** Next.js 16 (React 19)
- **Язык:** JavaScript (JSX)
- **Стили:** Tailwind CSS v4
- **UI компоненты:** shadcn/ui
- **Иконки:** Lucide React

### Backend
- **Runtime:** Node.js
- **Framework:** Express.js 5
- **БД:** MongoDB + Mongoose ODM
- **Аутентификация:** JWT (JSON Web Tokens)
- **Хеширование:** bcryptjs
- **CORS:** Настроено для локального и production окружения

### База данных (MongoDB)

**Коллекции (таблицы):**

1. **Users** - пользователи системы
   - `telegram` (уникальный)
   - `password` (хешированный)
   - `createdAt`

2. **Volunteers** - волонтеры
   - `name` - ФИ
   - `telegram` - контакт
   - `phone` - телефон
   - `skills` - навыки
   - `joinDate` - дата присоединения
   - `events` - массив событий (связь)

3. **Events** - события
   - `name` - название
   - `date` - дата события
   - `description` - описание
   - `location` - место проведения
   - `volunteers` - массив волонтеров (связь)

---

## API Endpoints

### Аутентификация
\`\`\`
POST /api/auth/register
POST /api/auth/login
\`\`\`

### Волонтеры (все требуют JWT токен)
\`\`\`
GET    /api/volunteers                    # Все волонтеры
POST   /api/volunteers                    # Создать волонтера
GET    /api/volunteers/:id                # Один волонтер
PUT    /api/volunteers/:id                # Обновить
DELETE /api/volunteers/:id                # Удалить
\`\`\`

### События (все требуют JWT токен)
\`\`\`
GET    /api/events                        # Все события
POST   /api/events                        # Создать событие
GET    /api/events/:id                    # Одно событие
PUT    /api/events/:id                    # Обновить
DELETE /api/events/:id                    # Удалить
\`\`\`

### Связь (Волонтер ↔ Событие)
\`\`\`
POST   /api/events/:eventId/volunteers/:volunteerId     # Добавить волонтера к событию
DELETE /api/events/:eventId/volunteers/:volunteerId     # Удалить волонтера из события
\`\`\`

---

## Как работает приложение?

### Поток данных

1. **Пользователь** открывает приложение на http://localhost:3000
2. **Frontend** отправляет запрос логина на Backend
3. **Backend** проверяет учетные данные в MongoDB и возвращает JWT токен
4. **Frontend** сохраняет токен в localStorage
5. **Frontend** отправляет токен в заголовке `Authorization` для всех запросов
6. **Backend** проверяет токен для защиты данных
7. **Frontend** отображает данные из MongoDB

### Адаптивность

- **Desktop (1024px+):** Полный навбар с меню
- **Мобила (<1024px):** Компактное меню с бургер-иконкой (≡)
- **Карточки:** Автоматически переходят с 1 на 2-3 колонки

---

## Основные функции

✓ Аутентификация через Telegram юзернейм  
✓ Полный CRUD для волонтеров  
✓ Полный CRUD для событий  
✓ Связь волонтеров с событиями  
✓ Адаптивный дизайн для мобилок  
✓ Поиск и фильтрация  
✓ JWT токены для безопасности  
✓ Хеширование паролей  

---

## Ключевые файлы для редактирования

### Если хочешь изменить стили
- `app/globals.css` - глобальные цвета и стили

### Если хочешь добавить новые поля в волонтера
- `components/volunteer-form.jsx` - форма
- `backend/server.js` - Volunteer schema

### Если хочешь добавить новый API endpoint
- `backend/server.js` - добавь `app.post(...)`, `app.get(...)` и т.д.

### Если хочешь создать новую страницу
- Создай файл в `app/` или `app/dashboard/` с расширением `.jsx`

---

## Версии зависимостей

**Frontend (package.json):**
- next: 16.0.0
- react: 19.2.0
- tailwindcss: 4.1.9

**Backend (package.json):**
- express: 5.1.0
- mongoose: 8.19.3
- bcryptjs: 3.0.3
- jsonwebtoken: 9.0.2

---

Всё готово! Начинай разрабатывать! 🚀
